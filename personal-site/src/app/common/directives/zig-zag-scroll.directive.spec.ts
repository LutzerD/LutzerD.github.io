import { ZigZagScrollDirective } from './zig-zag-scroll.directive';

describe('ZigZagScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ZigZagScrollDirective();
    expect(directive).toBeTruthy();
  });
});
